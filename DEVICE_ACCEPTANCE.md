# 真机验收记录（尚未执行）

当前发现 Xcode 27.0 (27A266a) 已安装，但 Apple 许可尚未接受。`xcodebuild` 与 `devicectl` 返回退出码 69，故无法构建、枚举设备或运行 UI 测试。先由用户打开 Xcode 阅读并接受许可、完成首次组件安装，再继续以下验收。不要将核心测试通过等同于真机通过。

设备要求：iOS 17 或更新，连接这台 Mac、解锁并信任电脑、启用 Developer Mode；Xcode 中选择 Apple Team，必要时改唯一 Bundle Identifier。无需购买服务或上架。

## 自动化验收

工程已包含 `FamilyTableUITests`，从 FamilyTable scheme 运行 ⌘U，或使用实际设备 ID：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project FamilyTable.xcodeproj -scheme FamilyTable -destination 'platform=iOS,id=YOUR_DEVICE_ID' test
```

界面测试包括早餐/晚餐数量与中文搜索、生成菜单/购买/重启持久化/未选位置不能收纳、菜谱列表滚动 CPU/内存/耗时采样。UI 测试存档使用独立 `FamilyTableUITests` 文件夹，不清空家庭真实数据。尚未执行，可能需要按真实系统 UI 行为调整查询。

## 必须在真机手动完成

| 场景 | 验收标准 | 状态 |
|---|---|---|
| 冷启动与重启 | 五个导航页可访问，数据保持，无崩溃 | 待测 |
| 5改6人、换餐 | 用量/采购即时更新，旧投票失效 | 待测 |
| 三个孩子投票 | 各自选择保留，家长能定稿 | 待测 |
| 中英搜索、70套列表 | 全部能打开，滚动无明显卡顿 | 待测 |
| 摄像头授权/拒绝/取消 | 可拍照；拒绝后说明和设置入口可用 | 待测 |
| 8张照片导入 | 方向正确、可放大查看、不中断界面响应 | 待测 |
| 人工确认同物同位 | 替换总量不重复累加，未确认量不扣除 | 待测 |
| 购买/撤销/实际收纳 | 状态区分，收纳一次只入库一次 | 待测 |
| 更改/删除储位 | 查询更新，未知位置明确显示待确认 | 待测 |
| 完成餐食 | 可扣量或不扣量；重复点击不再扣除 | 待测 |
| 飞行模式 | 所有本地核心流程可用，外部安全链接除外 | 待测 |
| 最大字号、小屏幕 | 无遮挡关键按钮、可滚动完成所有操作 | 待测 |
| 低存储空间保存失败 | 不误报成功，旧存档保持，能重试 | 待测 |
| 30分钟连续浏览操作 | 用 Instruments 检查卡顿/内存增长/崩溃 | 待测 |

要记录设备型号、iOS版本、构建号、测试时间、截图、崩溃日志及 Instruments 结果。没有这些实际证据前，不承诺“真实 iPhone 全功能通过、流畅、没有 bug”。
