# 真机验收记录（尚未执行）· Device acceptance (not yet run)

Family Kitchen 0.4。

编译已不再是阻塞项：Xcode 27 可以构建整个 App（`** BUILD SUCCEEDED **`），但系统默认开发路径仍指向 Command Line Tools，所以命令里要显式写 `DEVELOPER_DIR`。

**模拟器已安装并跑过**（iOS 27.0 runtime，iPhone 17）：14 条界面测试**全部通过**，其中"说一句/输入一句"的 6 条全部通过。

这一轮修测试时发现并修掉了一个真实缺陷：添加菜品页的用量输入用数字键盘，没有回车键，收不起键盘也就点不到"保存"。现已在键盘上方加了 **Done · 完成**。

模拟器**覆盖不到**的两件事，仍然只能在真机上验收：相机，以及语音识别本身——模拟器没有离线识别语言包，界面测试是把转写文本直接交给应用（见 `VoiceListener.scriptedReadings`）。也就是说"听到什么之后做得对不对"已经验证，"能不能听对"尚未验证。不要把模拟器通过等同于真机通过。

设备要求：iOS 17 或更新，连接这台 Mac、解锁并信任电脑、启用 Developer Mode；Xcode 中选择 Apple Team，必要时改唯一 Bundle Identifier（当前为 `com.familykitchen.app`）。无需购买服务或上架。

## 自动化验收

工程已包含 `FamilyKitchenUITests`，从 FamilyKitchen scheme 运行 ⌘U，或使用实际设备 ID：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project FamilyKitchen.xcodeproj -scheme FamilyKitchen -destination 'platform=iOS,id=YOUR_DEVICE_ID' test
```

界面测试包括早餐/晚餐数量与中文搜索、生成菜单/购买/重启持久化/未选位置不能收纳、输入中文指令后确认并使采购项落到底部、菜谱列表滚动 CPU/内存/耗时采样。语音只能在真机上验收：模拟器没有麦克风，离线识别语言包也不一定存在。UI 测试存档使用独立 `FamilyKitchenUITests` 文件夹，不清空家庭真实数据。尚未执行，可能需要按真实系统 UI 行为调整查询。

## 必须在真机手动完成

| 场景 | 验收标准 | 状态 |
|---|---|---|
| 冷启动与重启 | 五个导航页（Today / Week / Recipes / Shopping / Kitchen）可访问，数据保持，无崩溃 | 待测 |
| 厨房名称 | 设置后显示在 Today 顶部与导航标题 | 待测 |
| 添加家庭成员 | 输入名字后"Add to the family"可点击并成功添加；回车同样可添加 | 待测 |
| 成人/孩子与年龄 | 份量随年龄变化，采购量同步更新 | 待测 |
| 客人与手动份量 | 份数与采购随之变化，说明文字一致 | 待测 |
| 孩子投票 | 各自选择保留，家长能定稿 | 待测 |
| 过敏原排除 | 推荐与换菜均不出现，主动选择时有明确提示 | 待测 |
| 冰箱/冷冻柜/储藏柜 | 最多各四台；可命名并写明房间；默认隔层（含上方冷冻室、果蔬抽屉、乳制品抽屉、门格）可改名、改温度、删除、新增；"简单一点"模式下冰箱仍分冷藏与冷冻 | 待测 |
| 旧文件升级 | 0.3 及更早保存的设备名称升级为独立设备记录，种类按隔层温度推断，库存位置不丢 | 待测 |
| 出门前核对 | 拍照后给出建议清单；未确认前清单不变；确认后已有项打勾并落到底部"Nothing to buy" | 待测 |
| 识别的诚实度 | 识别不出时给出明确说明而非静默；识别词与把握程度与建议一同显示；数量可手改 | 待测 |
| 语音听写（中文/英文） | 麦克风与语音识别授权后，中英文各说一句都能被听懂；两种语言不需要事先切换 | 待测（模拟器无法验证） |
| 两种读法的取舍 | 一句中文被英文识别听成胡话时，采信听懂的那一只耳朵；"另一种听法"可点，点后按另一种读法重新解读 | **模拟器已通过**（转写文本由测试注入） |
| 语音只在本机 | 关闭网络（飞行模式）后语音仍可用；缺少离线语言包时给出明确说明与设置路径，而不是改走服务器 | 部分：模拟器确实缺少语言包，应用如实说明并可改为打字（已通过）；飞行模式下真机可用性待测 |
| 说一句改清单 | 说"家里已经有胡萝卜了"后先显示中英文理解结果；未点确认前清单不变；确认后该项打勾并落到底部"Nothing to buy" | **模拟器已通过**（转写文本由测试注入） |
| 说一句换菜 | 指定某天某餐换成某道菜后，只有那一餐改变，投票清空且需要重新确认；说"换一个"时给出推荐 | **模拟器已通过**（转写文本由测试注入） |
| 语音的诚实度 | 一词对应多道菜时列出候选让人选；完全不认识的菜名如实说明而不是随便换一道；早餐菜不能放进晚餐 | **模拟器已通过**（转写文本由测试注入） |
| 撤销 | 最近一次语音/文字改动可当场撤销，撤销后清单与菜单回到改动前 | **模拟器已通过**（转写文本由测试注入） |
| 拒绝麦克风授权 | 说明清楚且可改为打字；不崩溃 | 待测 |
| 白天/夜间/跟随系统 | 三种模式下文字与色块对比度可读，图标不变色 | 待测（真机观感） |
| 到点自动切换 | "按时间"模式下，应用开着不动，到了设定的整点自己切换 | **模拟器已通过**（17:45 浅色启动，18:00:39 自行转为深色） |
| 跟随系统实时切换 | "跟随系统"模式下，手机在应用运行中切换深浅色，应用随之改变 | **模拟器已通过** |
| 菜谱语言切换 | 中文/英文/双语即时生效，步骤与安全温度完整 | 待测 |
| 采购勾选与取消 | 勾选后可再点取消；已收纳的不可取消并有说明 | 待测 |
| 自建菜品 | 手动添加可保存、排入周计划、计入采购与营养 | 待测 |
| 网页导入 | 真实菜谱网址可解析；无结构化数据的网页给出明确提示而非失败静默 | 待测 |
| 吃过什么 | 已完成与已排入区分清楚；重复菜被推后 | 待测 |
| 中英搜索、76 套列表 | 全部能打开，滚动无明显卡顿；应季筛选正确 | 待测 |
| 摄像头授权/拒绝/取消 | 可拍照；拒绝后说明和设置入口可用 | 待测 |
| 8张照片导入 | 方向正确、可放大查看、不中断界面响应 | 待测 |
| 人工确认同物同位 | 替换总量不重复累加，未确认量不扣除 | 待测 |
| 购买/撤销/实际收纳 | 状态区分，收纳一次只入库一次 | 待测 |
| 更改/删除储位 | 查询更新，未知位置明确显示待确认 | 待测 |
| 完成餐食 | 可扣量或不扣量；重复点击不再扣除 | 待测 |
| 飞行模式 | 所有本地核心流程（含语音）可用，外部安全链接除外 | 待测 |
| 最大字号、小屏幕 | 无遮挡关键按钮、可滚动完成所有操作 | 待测 |
| 低存储空间保存失败 | 不误报成功，旧存档保持，能重试 | 待测 |
| 30分钟连续浏览操作 | 用 Instruments 检查卡顿/内存增长/崩溃 | 待测 |

要记录设备型号、iOS版本、构建号、测试时间、截图、崩溃日志及 Instruments 结果。没有这些实际证据前，不承诺“真实 iPhone 全功能通过、流畅、没有 bug”。
