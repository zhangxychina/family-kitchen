// Signing settings belong to whoever builds the app, not to the repository.
//
// Put your Apple Developer team in Signing.local.xcconfig next to this file — copy
// Signing.local.example.xcconfig and fill it in. That file is git-ignored, so the team
// that signs your builds never ends up in a public commit.
DEVELOPMENT_TEAM =

#include? "Signing.local.xcconfig"
